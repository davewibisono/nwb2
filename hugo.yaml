baseURL: "https://stately-axolotl-7242f5.netlify.app/"
languageCode: "en"
title: "NWB"

paginate: 6

# Markup 
markup:
  goldmark:
    renderer:
      unsafe: true

# Modules
module:
  mounts:
    - source: "assets"
      target: "assets"